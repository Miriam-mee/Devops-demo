name: Devops Test for firefox

on:
  push:

jobs:
  firefox:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Cypress run
        uses: cypress-io/github-action@v5
        with:
          node-version: 14
          browser: firefox